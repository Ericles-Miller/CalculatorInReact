import Calculator from './Calculator'

function App() {
  return (
    <div 
      className='fixed top-0 left-0 w-full h-full flex items-center justify-center'
      style={{ backgroundColor: '#807ECE' }}
    >
      <Calculator />
    </div>
  )
}

export default App
